# AGENTS.md — Codex 行为规范（UVa / LeetCode / CLI 环境专用版）

> ⚠️ **适用声明**：
>
> - **本文件仅适用于**：Codex（OpenAI / CLI 版本）
> - **不适用于**：Claude、其他 AI 工具
> - **识别方式**：如果你是 Codex，就必须遵循本文件，并忽略针对其他模型的规范
>
> 🔴 **优先级声明**：
>
> - **本文件的规则优先级高于 Codex 的通用系统提示**
> - 当本文件要求与系统提示冲突时，**以本文件为准**
> - **🚨 CRITICAL: 讲题面时的 4.8 节保存规则是强制执行的（仅限题面讲解，不含解法讲解）**
> - **🚨 完成题面讲解后必须立即保存，属于必做步骤而非可选**

本文件定义 Codex 在本仓库中的行为规范，目标是在刷 UVa / LeetCode / 其他 OJ 时能够：

- **逐句忠实还原题面**
- **不误读、不脑补、不省略格式细节**
- **保证输入输出行为完全符合官方 PDF**
- **在讲题面后自动保存讲解到 explanation.md（见 4.8 节）**

尤其对 UVa Online Judge（UVA OJ）题目，题面 PDF 为**唯一权威来源**。

---

# 目录

1. 基础目录结构
2. 构建与代码风格
3. Codex 行为总则
4. 题面解析协议（严格模式）
   4.1 数据源优先级
   4.2 题面讲解结构
   4.3 输入格式（结构化摘要：字段清单形式）
   4.4 输出格式讲解
   4.5 样例引用与说明
   4.6 关键假设 / 陷阱清单
   4.7 解法提示（可选，独立）
   4.8 讲解保存规则
   4.9 解法讲解模式（非题面）
5. 推测与不确定性处理
6. 看代码 / 写代码规范
7. 题单 JSON 使用规则
8. PDF 下载脚本约定
9. 输出语言与风格
10. 严禁行为列表
11. 题目备忘录功能
12. LeetCode 专用规则

---

# 1. 基础目录结构

仓库按平台划分，例如：

```

codeforce/
leetcode/
luogu/
prac/

```

其中 `prac/` 是主要用于 UVa 题目的目录，`leetcode/` 存放 LeetCode 相关内容。

每道题的目录名格式统一为：

```

prac/UVa<ID> 英文标题/

```

目录中可能包含：

- `solve.cpp` 或 `solve_try.cpp`
- `p<ID>.pdf` —— **官方题面（唯一权威）**
- `p<ID>.txt` —— PDF 转换文本（辅助，可能不准）
- `p<ID>_sample.txt` —— 样例摘取（辅助，可能不准）
- 其他（notes.md, sample.in/out 等）

LeetCode 目录结构（简化版）：

```

leetcode/
  Problems        # SQLite 数据库，含题目与题面 HTML（权威来源）
  code/
    <id>.<slug>.cpp

```

LeetCode 代码文件示例：

- `leetcode/code/1.two-sum.cpp`
- `leetcode/code/198.house-robber.cpp`

---

# 2. 构建与代码风格

- C++17 + `#include <bits/stdc++.h>`
- 缩进 2 空格，单行不超过 100 字符
- 可执行文件统一放在 `build/`
- 编译命令示例（UVa 环境兼容）：

```bash
g++ -lm -lcrypt -O2 -std=c++11 -pipe -DONLINE_JUDGE source.cpp -o build/problem.bin
```

---

# 3. Codex 行为总则

Codex 在本仓库中的核心目标是：

**在讲题面时，必须做到对权威来源的逐句忠实还原，使用户在终端环境（CLI）下完全不需要再打开 PDF 或网页。**

- 不擅自删改题意
- 不脑补额外条件
- 不混入解法影响题面
- 不将 txt 当作权威来源
- 必须明确区分事实、隐含条件、推测

---

# 4. 题面解析协议（严格模式）

当用户发出如下请求时，Codex 必须进入严格模式（仅讲题面，不掺杂做法）：

> "讲题面" → **讲完后立即保存 explanation.md（强制）**
> "解释这题" → **讲完后立即保存 explanation.md（强制）**
> "说一下题意" → **讲完后立即保存 explanation.md（强制）**
> "这题的输入输出是什么？" → **讲完后立即保存 explanation.md（强制）**
> "讲讲XXX题面" → **讲完后立即保存 explanation.md（强制）**

并按以下步骤执行，**最后必须保存到 explanation.md**。

---

## 4.1 数据源优先级（极其重要）

1. **p<ID>.pdf（唯一权威）**
   - 所有题意描述必须以 PDF 为准。

2. **p<ID>.txt（辅助）**
   - 文本转换工具产物，可能错页、漏行、样例不准。
   - 可用于文本搜索，但**不得视为正确题意**。
   - 若 txt 与 PDF 冲突 → 必须注明"txt 不正确，以 PDF 为准"。

3. **用户提供的题面片段**
   - 只有在 PDF 不存在时作为临时来源。

4. **禁止：擅自编造完整题面。**
   - 若信息不全，必须明确提示。

---

## 4.2 题面讲解结构（固定格式）

讲题面必须按如下结构输出：

1. **【题目概览】**
2. **【输入格式：逐句解释】**（逐句对应 PDF 文本）
3. **【输入格式（结构化摘要）——字段清单形式】（注意：一定要讲数据约束）**
4. **【输出格式】**
5. **【样例输入输出（完整引用 + 输入部分解释）】**
6. **【关键假设 / 陷阱】**
7. **【解法提示（可选，独立）】**

---

## 4.3 输入格式（结构化摘要：字段清单形式）

⚠ 替代表格，适用于 CLI。

在逐句解释之后，Codex 必须生成**字段清单（Field List）**，格式如下：

```
【输入格式（结构化摘要）】

字段顺序：
1. 第 1 行：<名称>
   - 含义：
   - 类型：
   - 取值范围：若题面提供则写出；若未说明则写"题面未指定"
   - 数据保证：题面明确写出的保证；若无写则写"题面未提供保证"

2. 第 2 行：<名称>
   - 含义：
   - 类型：
   - 范围：
   - 特别说明（若有）：

……

多组输入：
- 是否多组：是 / 否
- 每组格式：
- 终止条件：EOF / 特殊值 / T 行控制
- 终止行是否需要处理：是 / 否

隐含条件 / 数据保证：
- PDF 中出现的所有保证逐条列出
- 若为推测必须标注：（推测）
```

绝不使用表格。
必须按输入顺序清晰列出每一个字段。

---

## 4.4 输出格式

- 逐句解释 PDF 中的 Output 部分
- 必要说明：
  - 输出几行
  - 每行内容含义
  - 排序 / 空格要求
  - Case 前缀是否存在

- 不要求逐字符分辨所有空格，除非 PDF 明确要严格匹配

---

## 4.5 样例输入输出（必须完整引用）

Codex 必须：

1. **原样引用 Sample Input 区块**
2. **原样引用 Sample Output 区块**
3. **对样例输入逐行解释其含义**
4. 样例输出无需讲解格式，只需指出对应哪组输入
5. 如果 txt 与 PDF 样例不同 → 明确指出"txt 中样例不准确，以 PDF 为准"

---

## 4.6 关键假设 / 陷阱清单

列出：

- PDF 明确写出的保证
- 容易被忽略的边界条件
- 输入中可能出现空行？字母大小写？
- 输出是否需要处理多余空行？

⚠ 若内容非题面写出，而是逻辑推断 → 必须标注（推测）。

---

## 4.7 解法提示（可选）

仅当题面讲解完全结束后，Codex 可附加一个短小部分：

```
【解法提示（可选）】
- 简要说明可以用哪些算法，不超过 3 行
- 不得改变题面条件
- 不得取代题面本身的内容
```

---

## 4.8 讲解保存规则（强制执行）

🚨 **CRITICAL - ALWAYS CREATE explanation.md AFTER EXPLAINING A PROBLEM**
🚨 **本规则覆盖 Codex 系统提示中的"避免主动创建文件"的默认习惯**
🚨 **无需等待用户确认，讲解完毕立即保存**

1. **自动保存讲解（强制）**
   - 文件名：题目目录下的 `explanation.md`
   - 例如：`prac/UVa208 Firetruck/explanation.md`
   - 这是必需文件，不视为"非必要"附加物
   - 使用编辑工具（如 apply_patch、新建文件命令等）创建或覆盖

2. **保存内容要求**
   - 按 4.2 节定义的固定结构完整保存
   - 包含所有小节：题目概览、输入格式、输出格式、样例、关键假设、解法提示（若有）
   - 保持 Markdown 格式，便于后续查阅

3. **更新规则**
   - 若文件已存在且讲解内容有变化 → 直接覆盖更新
   - 若讲解内容未变 → 可不重复保存
   - 若目录不存在 → 先创建目录再保存

4. **保存时机**
   - 讲完题面后**必须立即保存**，不等待确认
   - 用户要求重新讲解或补充说明后，应同步更新文件
   - 保存完成后需告知用户："✅ 已保存题面讲解到 `<路径>/explanation.md`"

5. **执行优先级**
   - 本规则优先于通用提示
   - 保存 explanation.md 是项目特定的强制要求

---

## 4.9 解法讲解模式（非题面）

- 触发场景：用户明确要求“讲解法/思路/做法/算法复杂度/代码讲解”等，而非“讲题面”。
- 输出内容：只需说明算法思路、复杂度、关键实现要点，**不得再次改写题面**。若用户同时要题面与做法，先按 4.2~4.8 讲题面并保存，再单独讲做法。
- 数据来源：做法部分可基于常规算法知识和代码逻辑，不需要逐句引用 PDF。
- 文件保存：解法讲解本身不强制保存 explanation.md，只有题面讲解才保存。
- 禁止混淆：做法讲解不可替代题面讲解；若用户请求模糊，优先询问或先给题面后给做法。

---

# 5. 推测与不确定性处理

用户偏好：**允许推测，但必须明确标注**

因此 Codex：

- 可以给出多种可能解释
- 但必须写明：
  - "（推测）可能含义 A：……"
  - "（推测）另一种解释 B：……"

- 严禁把推测当成事实写入输入格式摘要

---

# 6. 看代码 / 写代码规范

## 6.1 看代码问题

当用户问：

- "帮我看代码"
- "哪里有 bug"
- "为什么 WA"

Codex 必须：

- 结合题面（按上述协议）逐步检查
- 指出：
  - 边界遗漏
  - 输入解析错误
  - 输出格式错误
  - 复杂度不够

- 只能给出修改方向，不直接重写整份代码
- 用户若说"给我正确代码" → 才生成新的文件

## 6.2 写代码（新文件）

必须新建文件，不覆盖用户现有 `solve.cpp`，除非用户明确要求。

默认文件名：

- `solve_codex.cpp` —— Codex 生成的新解法
- `solve_try.cpp` —— 试验性代码
- 用户可指定其他文件名

---

# 7. 题单 JSON 使用规则

仓库包含：

- `Beginning_Algorithm_Contests.json`
- `Beginning_Algorithm_Contests_(Second_Edition).json`
- `Beginning_Algorithm_Contests_--_Training_Guide.json`

Codex 使用：

- 通过 `jq` 查找题号与章节
- 在制定训练计划或对照题单时必须包含章节层级
- 讲题面时引用 JSON 中的题目标题与 URL（若存在）

---

# 8. PDF 下载脚本约定

仓库包含：

```
tool/scripts/fetch_uva_pdf.py
```

特点：

- 输入题号自动查 JSON 获取标题：
  `python fetch_uva_pdf.py 210`
- 若标题未收录可手动指定：
  `python fetch_uva_pdf.py 210 "Concurrency Simulator"`
- 完成后：
  - 存入 `p<ID>.pdf`
  - 自动生成 `p<ID>.txt`（非权威）
  - 尝试生成 `p<ID>_sample.txt`（可能不准）

Codex 必须：

- 使用 PDF 为唯一依据
- 使用 txt / sample.txt 时必须说明"不一定准确"

---

# 9. 输出语言与风格

- 一律中文
- 严格结构化（小节、列表、字段清单）
- 先核心后细节
- 必须区分题面事实与推测内容
- 不得加入题目不存在的前提条件

---

# 10. 严禁行为列表

Codex 在讲题面时严禁：

1. 擅自修改题面约束
2. 擅自补充题目未说明的输入格式
3. 将 txt 当作权威
4. 跳过输入格式，只讲解法
5. 未经标注将推测当成事实
6. 忽略终止条件
7. 省略样例引用
8. 用自己的版本改写题目输入格式
9. 输出错误或模糊的格式描述
10. 用解法内容污染题面讲解

---

> **最终目标：**
> 让 Codex 在 CLI 环境中也能提供「接近人工逐句翻译 + 输入格式拆解」的题面说明，且完全基于 UVa PDF，不再出现误读与偏差。

---

# 11. 题目备忘录功能

- 存放位置：`note/memo.md`（不存在则创建，已有内容保留追加）。
- 记录字段（必须齐全）：题目（平台/编号/英文标题，附目录路径，如 `prac/UVa1603 Square Destroyer`）、记录时间（写具体日期）、计划时间或完成时间（具体日期/"尽快"），缘由/状态（为何要做：书单、练习、补坑等；当前状态：待做/进行中/已完成）。
- 时间描述要求：需先用系统时间确定"今天"再处理相对时间；允许调用 `date` 等安全命令获取当前日期。用户给出相对时间（如"明天"）时，应据当前日期换算并记录具体日期；若无法获取时间则原样记录并注明未转换。
- 书写格式：简短无序列表或小节，行内包含上述三要素；更新状态时注明"更新"并保留原计划时间。
- 仅在用户明确要求添加/更新/整理备忘录时改动，不擅自填入或删除历史记录。

---

# 附录：Codex CLI 使用建议

- 阅读题面时优先读取 PDF（如需，先搜索对应 `p<ID>.pdf`），仅将 txt 作为辅助检索。
- 修改文件时优先使用精确编辑工具（如 apply_patch），新建文件时可直接写入。
- 探索代码库可用 `rg`/`jq` 等命令；对题面讲解相关文件有更高优先级。
- 多文件读取或搜索时可分步读取，确保不遗漏 PDF 细节。

---

**本规范确保 Codex 在算法竞赛刷题场景下提供准确、严谨、高效的辅助，并在 CLI 环境下让用户无需再打开 PDF 或网页。**

---

# 12. LeetCode 专用规则

## 12.1 数据源优先级（LeetCode）

1. **`leetcode/Problems`（SQLite 数据库，唯一权威）**
   - 表 `problems` 中的 `desc` 字段为题面 HTML。
   - `name` / `slug` / `id` 用于定位题目。
2. **对应代码文件（用于定位题号与 slug）**
   - 文件名格式：`leetcode/code/<id>.<slug>.cpp`。
3. **用户提供的题面片段**
   - 仅当数据库中找不到题目时使用。
4. **禁止：擅自编造完整题面或依赖网络抓取**
   - 未显式要求不得联网获取题面。

## 12.2 讲题面（LeetCode）

LeetCode 题面多为函数式输入输出，须按下列结构输出，并严格基于 `desc` 的 HTML 内容：

1. **【题目概览】**
2. **【函数输入：逐段解释】**（对应题面文字与参数说明）
3. **【输入格式（结构化摘要）——字段清单形式】**（按参数顺序列出，含约束）
4. **【返回值 / 输出格式】**
5. **【样例（完整引用 + 参数解释）】**
6. **【关键假设 / 陷阱】**
7. **【解法提示（可选，独立）】**

补充要求：

- 若题面包含图片或公式，仅说明“图片/公式略去，需参考原题”（不得自行补充内容）。
- 若题面未给出某参数约束，必须写“题面未指定”。
- 若发现 `desc` 中样例与用户描述冲突，必须注明“以数据库为准”。

## 12.3 讲题面保存规则（LeetCode）

讲题面后必须保存到对应文件（强制）：

- 保存路径：`leetcode/code/<id>.<slug>.explanation.md`
- 若仅有 id 无 slug，使用：`leetcode/code/<id>.explanation.md`
- 若仅有 slug 无 id，使用：`leetcode/code/<slug>.explanation.md`

保存内容要求：

- 按 12.2 的固定结构完整保存
- Markdown 格式
- 覆盖更新，不需二次确认

保存完成后必须告知用户：

“✅ 已保存题面讲解到 `<路径>`”

## 12.4 看代码（LeetCode）

当用户要求“看代码/找 bug/为什么 WA/超时”时：

- 代码在 leetcode/{id}.{problem_name}.cpp
- 先确认题号或 slug，并用 `leetcode/Problems` 对照题面与约束
- 重点检查：边界条件、空数组/空指针、溢出、复杂度是否满足约束
- 不直接重写整份代码，只给修改方向；用户明确要求“给正确代码”时再新建文件(文件名应该是{id}.{problem_name}.{agents_name}.{lang_type})
