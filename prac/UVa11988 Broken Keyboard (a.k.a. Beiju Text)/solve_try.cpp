#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int l = 0;
    cin >> l;
    while (l--) {
        int I, D;
        cin >> I;
        cin >> D;
        vector<bool> tree((1 << D) - 1);
    }
}
